<% if notice %>
  <div class="max-w-xl mx-auto">
    <div class="flex items-center mx-auto justify-center rounded-sm border-2 border-green-500 text-black">
      <p class="font-semibold text-lg p-4">
        <%= flash[:notice]  %>
      </p>
    </div>
  </div>
<% end %>